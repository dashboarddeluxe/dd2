[build]

command = "npm run production"
publish = "build_production"
environment = { PHP_VERSION = "7.4" }

[[headers]]
  for = "/*"
  [headers.values]
    cache-control = '''
    max-age=300,
    no-cache,
    no-store,
    must-revalidate'''
